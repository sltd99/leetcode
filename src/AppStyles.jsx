import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles(theme => ({
  root: {
    width: "100%"
  },
  container: {
    marginLeft: 10,
    marginRight: 10
  }
}));
